# utils/

Small, dependency-free helpers shared across services/controllers
(id generation, seeded RNG for deterministic mock data, unit conversions).
Added starting Phase 3 as the hazard generator needs them.
