# controllers/

Thin request handlers: parse the request, call a service, shape the
response. No business logic lives here.

- `hazardController.js` (Phase 3)
- `routeController.js` — validate-route / generate-route (Phase 4-5)
- `missionController.js` (Phase 5)
